import React from "react";

const Title = () => {

    return (

        <div className = "title">
            <h2 className = "title-name">
                NoRi Blog
            </h2>
        </div>

    );
};

export default Title;